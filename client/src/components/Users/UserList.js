import React, { useEffect } from 'react';

const UserList = () => {
    const [data, setData] = useState(null);
    useEffect(() => {
        effect
        return () => {
            cleanup
        }
    }, [input])
}
export default UserList;