import React, { useEffect } from 'react';

const ProjectList = () => {
    const [data, setData] = useState(null);
    useEffect(() => {
        effect
        return () => {
            cleanup
        }
    }, [input])
}